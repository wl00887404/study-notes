#include <iostream>
#include <vector>

using namespace std;

/**
 * 數字範圍超過 index
 * 不能在 index 上動手腳
 * 長度一定是 2n + 2
 * 
 */

class Solution {
 public:
  vector<int> singleNumber(vector<int>& nums) {
    int size = nums.size();
    if (size = 2) return nums;

  }
};